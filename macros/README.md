# lower-ir-utils-macros

Proc-macro crate for [**lower-ir-utils**](https://crates.io/crates/lower-ir-utils): `#[jit_export]` and helpers. Prefer depending on **`lower-ir-utils`**; this crate is published separately only so the facade can stay dependency-light outside macro expansion.
